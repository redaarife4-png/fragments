backend:
  name: git-gateway
  branch: main

# Editorial workflow enables drafts, review, and publish stages
# (matches the "brouillons" workflow you asked for)
publish_mode: editorial_workflow

# Where uploaded images/videos are stored
media_folder: "public/uploads"
public_folder: "/uploads"

# Site metadata (shown in preview links)
site_url: https://redarif.netlify.app
display_url: https://redarif.netlify.app

# UI language of the CMS itself
locale: 'fr'

collections:
  - name: "posts"
    label: "Articles"
    label_singular: "Article"
    description: "Essais, carnets de voyage, notes courtes."
    folder: "src/content/posts"
    create: true
    slug: "{{slug}}"
    preview_path: "essai/{{slug}}"
    summary: "{{title_fr}} · {{category}} · {{date | date('DD/MM/YYYY')}}"
    fields:
      - label: "Titre (FR)"
        name: "title_fr"
        widget: "string"
      - label: "Titre (EN)"
        name: "title_en"
        widget: "string"
      - label: "Date de publication"
        name: "date"
        widget: "datetime"
        format: "YYYY-MM-DD"
        date_format: "DD/MM/YYYY"
        time_format: false
      - label: "Catégorie"
        name: "category"
        widget: "select"
        options:
          - { label: "Essai", value: "essai" }
          - { label: "Carnet de voyage", value: "voyage" }
          - { label: "Note courte", value: "note" }
          - { label: "Avis", value: "avis" }
      - label: "Chapeau (FR)"
        name: "dek_fr"
        widget: "text"
        required: false
        hint: "Sous-titre italique affiché sous le titre de l'article"
      - label: "Chapeau (EN)"
        name: "dek_en"
        widget: "text"
        required: false
      - label: "Extrait (FR)"
        name: "excerpt_fr"
        widget: "text"
        hint: "Résumé court affiché sur la page d'accueil (2-3 phrases)"
      - label: "Extrait (EN)"
        name: "excerpt_en"
        widget: "text"
      - label: "Corps de l'article (FR)"
        name: "body_fr"
        widget: "markdown"
      - label: "Corps de l'article (EN)"
        name: "body_en"
        widget: "markdown"
